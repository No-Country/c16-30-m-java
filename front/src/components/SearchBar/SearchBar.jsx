import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input type="search" placeholder="Productos" autocomplete="off" />
      <button>Buscar</button>
    </div>
  );
};

export default SearchBar;
// Modificacion
